import 'package:flutter/material.dart';

class UserProfileViewWeb extends StatelessWidget {
  const UserProfileViewWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("data"),
    );
  }
}
