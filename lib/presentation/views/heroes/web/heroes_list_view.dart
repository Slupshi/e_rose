import 'package:flutter/material.dart';

class HeroesListViewWeb extends StatelessWidget {
  const HeroesListViewWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("HeroListViewWeb"),
    );
  }
}
